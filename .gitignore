# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Data & model artifacts (too big / not source) — teammates download/produce these
data/
data_*/
predictions.json
sample_images/

# NOTE: *.pth is intentionally NOT ignored here — the trained model.pth /
# model_all.pth / model_baseline.pth are committed so the demo & grader work.

# OS / editor
.DS_Store
.idea/
.vscode/
